#Bad type assigned to component
component Stats:
    int age
    float height
end

component Name:
    string name
end

entity Person:
    Stats
    Name
end

function void main(<string> args):
    Person.Stats.age = (2 * (2 + 4))
    Person.Stats.height = (2.0 * (2 + 4))
    Person.Stats.length = 34.0
    #Person.Color.color = "red"
    #Random.Name.name = "hi"
end
