function void main(<string> args):
    <string> x
    <string> y
    x = ["a", "b", "c", "f"]
    y = slice(x, 1, 3)
    # should return ["b", "c"]
    printi(len(y))
    prints(y[0])
    prints(y[1])

    y = slice(x, 0, 4)
    # should return ["a", "b", "c", "f"]
    printi(len(y))
    prints(y[0])
    prints(y[1])
    prints(y[2])
    prints(y[3])
    
end

